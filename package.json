{
  "name": "my-html-app",
  "version": "1.0.0",
  "description": "HTML to APK converter",
  "author": "Moder_heba",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0"
  }
}